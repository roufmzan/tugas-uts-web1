# Assets

- Tempatkan logo website di file `logo.png` di folder ini.
- Path yang disarankan untuk logo: `assets/logo.png`.
- Format yang didukung: PNG/SVG/JPG (disarankan PNG/SVG).

Catatan:
- Ukuran ideal logo navbar: tinggi 36px (akan diskalakan via CSS).
- Nama file lain bebas, tetapi dokumentasi dan contoh akan menggunakan `assets/logo.png`.
